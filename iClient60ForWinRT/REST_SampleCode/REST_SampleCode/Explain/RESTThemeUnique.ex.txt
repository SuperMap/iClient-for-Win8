单值专题图是利用不同的颜色或符号（线型、填充）表示图层中某一属性信息的不同属性值，属性值相同的要素具有相同的渲染风格。

制作一个专题图，即在服务端增加一个资源，每个资源拥有一个 ID 号和一个 url 地址。显示专题图的过程就是从服务器返回结果
 ThemeResult.resourceInfo 中获取到这个资源的 ID 号，然后将该值赋值给已实例化的动态图层（TiledDynamicRESTLayer 或
  DynamicRESTLayer）的属性 layersID，即完成专题图的显示。

移除专题图是在客户端实现，不与服务器交互。

